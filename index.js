const divide = (parameter1, parameter2) => (parameter1/parameter2);
let parameter1 = 2000
let parameter2 = 100

const square = prm1 => (prm1*prm1)
let prm1 = 2

const add = (prm2,prm3) => (prm2 + prm3)

